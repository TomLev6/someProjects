from Filters import RateLimiter
import socket


"rate_limiter = RateLimiter()"

# Check if the request is allowed
"if rate_limiter.add_request(client_ip):"
# Serve the request
"else:"
# Reject the request
